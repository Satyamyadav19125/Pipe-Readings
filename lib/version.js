// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 21;
export const APP_LEVEL_NOTES = 'Raw Kobo view + UID search + filters everywhere; outside-height column fix; period-aligned stale flag; far-from-project GPS flag (Sidhuwal); active pipe/farm counts; start-time prefill; de-duplicated detail; multi-sheet exports; clean/raw/dead tabs; mini-maps on submissions & assignment details';
