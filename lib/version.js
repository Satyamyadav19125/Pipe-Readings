// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 30;
export const APP_LEVEL_NOTES = 'Map crash/lag fixed — every pin drawn on ONE canvas (fast, all pins shown); irrigation status filters hidden behind the 💧 Irrigation button again (show only on tap); faster Submissions page (dropped a redundant second Kobo fetch — Raw = untouched forms which are already raw).';
