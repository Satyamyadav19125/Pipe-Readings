// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 22;
export const APP_LEVEL_NOTES = 'Paginated Kobo fetch (past the 1000-row cap — now unlimited); "you are here" marker on the how-far-am-I map; new Clean tab (accurate/fixed readings); per-tab downloads aligned with admin-verified; note prompt when marking a reading dead; consistent date-range filtering.';
