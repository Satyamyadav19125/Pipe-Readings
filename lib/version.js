// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 27;
export const APP_LEVEL_NOTES = 'Guest hardened: public landing auto-generic when guest is on (stripping /view leaks nothing); guest village count fixed, demo photos render, demo chat + demo tasks (with the admin task UI shown read-only), no guest profile; Globe now uses selectable map tiles (Street/Satellite/Topo) + fixed "map data not available" (zoom cap); edit-form uses date/time pickers + surveyor dropdown; clearer All-tab explanation.';
