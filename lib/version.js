// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 28;
export const APP_LEVEL_NOTES = 'Root landing back to your FULL branded page (only /view is generic for guests); Raw tab is now a real subset (untouched forms) so All > Raw; Globe fixed — proper textured sphere, no overlap/corruption, filters + layer swap work; pins always show when zoomed in; nicer illustrated demo photos; editable + saved guest profile.';
