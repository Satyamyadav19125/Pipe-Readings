// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 23;
export const APP_LEVEL_NOTES = 'How-far map now follows the ROADS (OSM routing) with road distance + Google Maps driving directions; assignment reading rows fit mobile and always show the farm ID; same-day duplicate readings surfaced with both forms side-by-side (new Duplicates tab + export scope), independent of red-flag toggles.';
