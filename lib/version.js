// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 29;
export const APP_LEVEL_NOTES = 'Map: clustering removed — every reading is pinned directly (no cluster animation); Globe removed; irrigation status filters are always visible (no extra tap); deep-zoom "map data not available" fixed (satellite upscales past its tile limit); guest demo forms now show REAL pipe photos borrowed from your Kobo data instead of illustrations.';
