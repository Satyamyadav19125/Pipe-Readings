// Build level — bumped once per delivered update so you can confirm at a
// glance which version is actually live on Vercel after a deploy.
export const APP_LEVEL = 31;
export const APP_LEVEL_NOTES = 'Map filters redesigned into ONE clear bar — "🚩 Flagged" now genuinely FILTERS the map to red-flagged pins (it was only a colour toggle before); irrigation is a filter option that swaps in its own status filters with a Back button; empty-filter note added so a 0-result filter no longer looks broken.';
